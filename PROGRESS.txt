SAFETECH PRECAST ERP — PROGRESS LOG
Last updated: 2026-07-11 (v2 ROADMAP COMPLETE — all 8 phases done and committed (677d011). Trailer test-01/A-Frame casing fixed so Trailer Loading Sim's lookup matches cleanly. User explicitly asked to SKIP populating the other 20 empty tables (Prestressing/Maintenance/Document Control/Handover/Environmental/Erection Planning/Formwork) for now — no placeholder data was invented, per this project's standing rule. Revisit only when real operational data is provided.)

=====================================================================
STACK / ENVIRONMENT
=====================================================================
- React 18 + TypeScript + Vite 5 + Tailwind CSS. Dark-theme red/black
  glassmorphism design system (glass-panel, glass-card-3d, glowing-input,
  btn-interactive, concrete-accent-*, glow-text-*, status-live,
  warning-card, safety-accent, rev-badge). App is light-by-default at the
  CSS level — every custom style must ship both light and dark: variants.
- Supabase (Postgres + Auth + RLS). Project ref umlrgsozzjjbwjonziid,
  URL https://umlrgsozzjjbwjonziid.supabase.co
- Canonical source: /mnt/Gemini Cli/build (git repo, Android SD-card mount,
  cannot run node natively — symlink restrictions).
  Toolchain copy: /root/build (ext4, where npm/vite/tsc actually run).
  Sync via tar, NOT cp/rsync with cd (caused failed syncs earlier):
    tar -cf /root/syncN.tar -C "<src>" --exclude=node_modules --exclude=.git --exclude=dist .
    tar -xf /root/syncN.tar -C /root/build
    rm -f /root/syncN.tar
  Always edit BOTH copies (or edit one, then sync) — changes must exist in
  both before considering a task done.
- CLAUDE.md lives at /mnt/Gemini Cli/build/CLAUDE.md and /root/build/CLAUDE.md
  — permanent dev context, scope boundaries, and the module target list.
  Its own rule: "Never continue automatically. Wait for approval before
  starting the next phase."
- LAN dev server: scripts/dev-lan.sh (kills/restarts via
  `pkill -f "vite --host"` then `sh scripts/dev-lan.sh` if the sidebar/nav
  doesn't pick up changes — Vite's watcher has been observed to silently
  miss edits to widely-imported files like registry.ts).
- Login: admin@safetech.ae / SafetechPhase2!  (also supervisor/controller/
  viewer/production-dept test users — see db/seed_test_users.sql)
- Claude Design project synced: "Safetech Precast ERP",
  projectId b09fcd1c-6cca-4d27-9b9e-f6bf00501631 (7 components synced,
  3 with authored previews). NOT re-synced since HSE/Workforce work —
  would need another /design-sync pass if new components should appear
  there.

=====================================================================
DONE — VERIFIED COMPLETE
=====================================================================
1. Applied db/init.sql, db/erp_schema.sql, db/rls_policies.sql to Supabase.
   db/phase2_acceptance.sh passing 7/7.

2. CLAUDE.md permanent context document authored and saved.

3. Earlier-session audit: removed localStorage/mock-DB usage across the
   codebase per CLAUDE.md's "single source of truth, no localStorage
   fallback, no mock database" rule.

4. /design-sync run — synced 7 real components to the Claude Design
   project above.

5. LAN hosting set up (scripts/dev-lan.sh), credentials provided.

6. Delivery Note / Delivery Report print-CSS fully debugged and fixed.
   Both now print as exactly 1 page (verified via Playwright
   page.pdf({preferCSSPageSize:true}) + pdftoppm PNG inspection).
   Key fixes: @page size/margin injection, fixed mm heights (h-[297mm]
   not min-h), print:break-inside-avoid, print:pl-0/p-0/max-w-none/mx-0
   on the shared layout wrapper, empirically-tuned page heights
   (print:h-[288mm] for Note, print:h-[411mm] for Report A3 view,
   print:h-[288mm] for Report traceability view).
   NOTE: traceability sub-view was not independently re-verified with
   real test data — spot-check if it becomes relevant again.

7. PHASE 1 ERP EXPANSION — "HSE + Workforce modules + ModuleWorkspace
   usability upgrade" (this was scoped as MY OWN prioritization from an
   open-ended "do all further required developments" instruction — see
   the approved plan for full reasoning). FULLY COMPLETE AND VERIFIED:

   a) DB migration db/hse_workforce.sql — applied live. 9 new tables,
      all with created_at/updated_at/created_by/updated_by (the standard
      the 58 PRE-EXISTING tables do NOT follow — see gap list below):
        hse_incidents, toolbox_talks, risk_assessments, hse_permits,
        safety_audits, crew_members, crew_assignments, shift_schedules,
        certifications (shared between HSE "Certifications" and
        Workforce "Certification Tracking" via cert_category column).
      RLS: per-(table,section) policies via has_permission(), NOT the
      placeholder "authenticated full access" policy the old tables use.
      RBAC seeded: admin full; supervisor full CRUD both sections;
      controller/viewer view-only; production dept gets workforce edit.
      Verified via list_tables / get_advisors(security) — no new warnings.

   b) registry.ts: SectionKey extended with 'hse' | 'workforce'. 9 new
      ModuleDef entries added to MODULES[] (full CRUD via the generic
      /m/:moduleId route — zero new component code needed). 2 new
      NAV_SECTIONS entries inserted between qaqc and stockyard.

   c) New pages: HSEDashboardPage.tsx (/hse) and
      WorkforceDashboardPage.tsx (/workforce) — KPI cards, Recharts donut
      charts, AlertsPanel, MiniTable, matching Dashboard.tsx's established
      "good design" pattern. Routed + ProtectedRoute-gated in
      AppRoutes.tsx.

   d) DashboardWidgets.tsx (new file) — extracted SectionTitle/MiniTable
      out of Dashboard.tsx so HSE/Workforce dashboards can reuse them.

   e) ModuleWorkspace.tsx usability upgrade — benefits ALL ~55 modules,
      old and new:
        - per-column filter row (in addition to existing global search)
        - bulk actions: checkbox column, bulk delete, bulk status-update
        - inline status quick-edit (click a status chip to edit in place,
          skips the full modal for the most common edit)
        - responsive card view below md breakpoint (was horizontal-scroll
          table only before)
        - table text bumped ~9-10px -> ~11.5px, more cell padding

   f) Sidebar.tsx — nav search box added (filters sections/items live),
      auto-expands sections that match a search.

   g) BUG FOUND AND FIXED (this is a real product bug, not new-module-
      specific — affects every module with an optional date field):
      ModuleWorkspace.tsx's saveForm() sent empty string '' for blank
      fields. Postgres date columns reject '' (only NULL or a valid date
      literal are legal) -> insert failed with
      `22007 invalid input syntax for type date: ""`. Fixed by coercing
      blank date-typed fields to null before the insert/update payload
      is built. Fixed in BOTH /root/build and the canonical mount copy.

   h) End-to-end Playwright verification against the LIVE LAN dev server
      (Chrome extension not connected this session, so all UI
      verification this whole session was via Playwright, launched from
      /root/build/.ds-sync/node_modules/playwright/index.mjs):
        - tsc --noEmit clean
        - vite build succeeds
        - RLS acceptance spot-check: viewer read-only / write-denied,
          supervisor write-allowed, admin has_permission() true — all as
          expected for both new sections
        - Created a real hse_incidents record through the Add-Record
          modal -> confirmed it appears in the table
        - Inline status quick-edit -> confirmed status changed
        - Bulk select + bulk delete -> confirmed row removed
        - Responsive mobile card view, light theme, sidebar nav search —
          all screenshot-verified correct

   DEFERRED FROM PHASE 1 (explicitly, not forgotten):
     - Prestressing, Document Control, Customer Handover, Sustainability
       modules (4 of CLAUDE.md's 17 target categories still fully unbuilt
       at the time Phase 1 was scoped)
     - Retrofitting updated_at/created_by/updated_by onto the 58
       PRE-EXISTING tables (only created_at today)
     - init.sql / erp_schema.sql duplicate-table-definition cleanup
     - Full mobile-first redesign beyond the ModuleWorkspace card fallback
     - Converting existing text-typed date columns to real date/timestamptz
   (NOTE: the audit-column retrofit and the 4 modules above are now DONE
   as of the v2 Phase 1 entry below — see item 8.)

8. V2 ROADMAP — PHASE 1: "Core Data Spine" (schema + RLS/permissions fix).
   User pasted a new 8-phase roadmap ("Full Execution Plan v2") and
   directed: (a) treat it as the new roadmap superseding the ad-hoc
   Phase 1/2 framing above (HSE/Workforce fold in as its own "Phase 5",
   already done); (b) start with this plan's own Phase 1; (c) since the
   plan referenced "18 missing tables" without ever listing them, DERIVE
   the real list by cross-referencing the v2 module names + CLAUDE.md's
   17 categories against the live schema, rather than trust the figure.
   FULLY COMPLETE AND VERIFIED:

   a) DERIVATION (before writing any code): pulled the authoritative
      table list straight from db/*.sql (grep for `create table`) instead
      of trusting the registry or the "18" figure. Found 3 tables the v2
      plan's LATER phases would have needed that ALREADY EXIST —
      production_casting (Phase 2), element_traceability (Phase 3),
      maintenance_logs (generic equipment log) — creating them would have
      duplicated live data. The real gaps were whole CATEGORIES with zero
      tables: Prestressing (§11), Site Erection (§8), plant-Maintenance
      (§9, partial — maintenance_logs already covers breakdown/general),
      Document Control (§12), Customer Handover/DLP (§14), Environmental/
      Sustainability (§15). User then chose (via AskUserQuestion): create
      ALL missing tables now (not just spine-critical ones), AND fold the
      RLS/audit-column retrofit into this same migration.

   b) CORRECTION mid-build: the plan assumed the 58 pre-existing tables
      were still on the placeholder "authenticated full access" RLS
      policy. Reading db/rls_policies.sql showed that was already
      replaced with real has_permission() policies for EVERY table
      (applied in an earlier session — db/phase2_acceptance.sh 7/7 was
      the tell). So the only genuine existing-table gap was the missing
      audit columns, not RLS. Dropped the redundant RLS-rewrite step
      rather than duplicate ~150 lines of already-correct policy logic.

   c) DB migration db/phase1_data_spine.sql — applied live via the
      Supabase MCP (see "MCP AUTH" note below for how that got unstuck).
      Idempotent (create table if not exists / add column if not exists
      throughout — safe to re-run, no data loss risk even against live
      data: elements had 26 rows, deliveries 961 rows, before the run).
        1. Audit-column retrofit: a generic do $$ loop over
           information_schema.tables adds updated_at/created_by/
           updated_by to EVERY existing public table that lacks them —
           the full 58-table gap from the old Phase 1 deferred list,
           closed in one idempotent pass, no table hand-listed.
        2. elements.shape_type added (Box/Solid Wall/Hollow-core/Beam/
           Column/Panel/Slab/Stairs/Custom) — the field Phase 6's 3D
           renderer will need to pick element geometry. elements' RLS
           was already real, untouched.
        3. 22 new tables across 6 new sections:
             prestressing: pt_strands, pt_tensioning, pt_release,
               pt_long_line_plans
             erection: erection_planning, erection_log,
               grouting_records, connection_inspections
             maintenance: equipment_register, preventive_maintenance,
               calibration_records (+ reused existing maintenance_logs,
               re-sectioned — see (d))
             documents: rfi_register, method_statements, submittals
             handover: handover_packages, dlp_records, handover_defects,
               customer_acceptance
             environmental: carbon_records, waste_records, water_records,
               environmental_reports
           Explicitly NOT created (avoids CLAUDE.md scope/dup violations):
           Spare Parts / raw-material stock tables (inventory-valuation-
           adjacent, out of scope); no new Batching/Formwork master
           tables (concrete_batches / moulds already cover those module
           names — v2's naming just didn't match the existing schema).
        4. RLS: same dynamic drop-and-recreate (table,section) loop
           pattern as rls_policies.sql/hse_workforce.sql, reusing
           has_permission() — for all 22 new tables + maintenance_logs.
        5. RBAC seed for the 6 new sections: admin full CRUD+approve;
           supervisor full CRUD on prestressing/erection/maintenance
           (shop-floor daily work); controller/viewer view-only on all
           six; documents/handover/environmental additionally granted to
           design dept (documents) and management dept (handover,
           environmental) — narrower than the operational sections since
           these are office-driven, not daily shop-floor work.

   d) maintenance_logs RE-SECTIONED from 'logistics' to the new
      'maintenance' section (it's a generic equipment log, not fleet-
      specific — belongs with the new category, not duplicated).
      Consistently updated in 3 places so nothing drifts out of sync:
      src/lib/erp/tableSections.ts (EXTRA_TABLE_SECTIONS), the migration's
      RLS loop, and src/AppRoutes.tsx's /maintenance route
      (ProtectedRoute section="logistics" -> "maintenance"). RBAC seed
      grants logistics dept edit on the new 'maintenance' section so it
      keeps the access it had before the move (no regression).

   e) registry.ts: SectionKey extended with 6 new keys (prestressing,
      erection, maintenance, documents, handover, environmental). 22 new
      ModuleDef entries (full CRUD via /m/:moduleId, zero new component
      code). shape_type select field added to the Element Register
      module. 6 new NAV_SECTIONS entries in lifecycle order (Prestressing
      after Production; Site Erection + Maintenance after Logistics;
      Document Control/Customer Handover/Environmental before Reports).

   f) MCP AUTH: the Supabase MCP was disconnected for most of this phase
      (apply_migration/execute_sql/list_tables/get_advisors all
      unavailable). First re-auth attempt failed with a real OAuth error
      ("Unrecognized client_id" — a broken client registration, not a
      transient issue). User then tried sharing raw credentials directly
      in chat (a live sb_secret_ Data API key, then a sb_publishable_
      key) — REFUSED to use these: (1) a Bash command embedding the
      literal secret got auto-blocked by the environment's safety
      classifier as credential leakage before it could run; (2) even if
      usable, PostgREST Data API keys of either tier cannot execute raw
      DDL (create table/alter table/do $$ blocks) — that capability
      doesn't exist over that API regardless of key privilege. Advised
      the user to rotate both keys since they're now in the chat
      transcript. Installed `psql` via apt-get as a Plan-C fallback
      (never used — user said "retry" instead). The SECOND MCP
      authenticate() call generated a fresh client_id/redirect port and
      that one worked — user completed the browser OAuth flow and pasted
      the localhost callback URL back, MCP reconnected cleanly. Lesson
      for future sessions: if Supabase MCP auth fails with "unrecognized
      client_id", retrying authenticate() (not complete_authentication on
      the stale URL) gets a fresh registration that may succeed.

   g) VERIFICATION (all passed):
        - tsc --noEmit clean, vite build clean (before applying the
          migration).
        - Post-migration list_tables: all 22 new tables present, RLS
          enabled on every one. role_permissions +24 rows (6 sections x
          4 roles, exact match), department_permissions +4 rows (exact
          match).
        - get_advisors(security): zero NEW warnings — every flagged item
          (audit_logs unrestricted insert, several SECURITY DEFINER
          functions, leaked-password protection) is pre-existing and
          unrelated to this migration.
        - pg_policies spot-check: 4 CRUD policies (SELECT/INSERT/UPDATE/
          DELETE) confirmed present on 8 sampled tables incl. elements
          (proves its pre-existing RLS was untouched) and maintenance_logs
          (proves the re-section applied).
        - Playwright E2E against the LIVE LAN dev server (11/11 checks
          passed): admin login; all 6 new sidebar sections render; nav
          search finds "RFI Register"; created a real record in
          pt_tensioning through the Add-Record modal and confirmed it
          lists; same for rfi_register; Element Register form shows the
          new "Shape" field. Test records deleted afterward via
          execute_sql — live tables confirmed back to 0 rows.
        - Dev server was restarted (registry.ts is the known HMR-flaky
          file) using the Bash tool's run_in_background flag — a manual
          `nohup ... &` attempt died silently first; run_in_background
          is the reliable way to background scripts/dev-lan.sh in this
          harness.

   DEFERRED FROM V2 PHASE 1 (explicitly, not forgotten):
     - Text-typed date columns (planned_cast_date, cast_date,
       qr_generated_at, and the same pattern across most pre-existing
       tables) still NOT converted to real date/timestamptz — existing
       rows may hold non-parseable text; app's date handling already
       tolerates text; not worth the data-migration risk in this pass.
     - init.sql / erp_schema.sql duplicate-table-definition cleanup.
     - All rich/custom UI for the 6 new sections (dashboards, KPI cards,
       bespoke workflows like Dashboard.tsx/HSEDashboardPage.tsx) — the
       22 new modules are generic CRUD only right now, by design; custom
       UIs are v2 Phases 2-8, not Phase 1's job.
     - The design-system conflict (next section) — still open, still not
       needed until Phase 7.

9. V2 ROADMAP — PHASE 2: Production Planning ("Cast Bed Plan"). Exploration
   corrected the premise first: production_casting is NOT unused —
   ProductionPage.tsx already has a working per-element casting-log UI
   writing to it with real downstream cascades. What was actually missing
   was a spatial/temporal SCHEDULING VISUALIZATION. The right entity to
   visualize is `casting_schedule` (schedule_date/shift/bed/status),
   which already had a full form+list UI in CastingSchedulePage.tsx
   (route /casting-schedule) with the complete lifecycle (Scheduled → QR
   Generated → In Progress → Completed/Cancelled) and correct downstream
   cascades. No 2D grid/calendar/drag-drop pattern existed anywhere in
   the codebase, and no dnd library was installed. FULLY COMPLETE AND
   VERIFIED:

   a) User decisions (via AskUserQuestion): build Cast Bed Plan as a
      SEPARATE new page (own route/nav entry) rather than a view-toggle
      on CastingSchedulePage.tsx — keeps that proven, shipped page
      completely untouched, zero regression risk on it. Add
      @dnd-kit/core + @dnd-kit/utilities as new deps (purpose-built
      draggable/droppable with real collision detection) rather than
      hand-rolling pointer-math on framer-motion. Leave the orphaned,
      unrouted dead-code file src/pages/CastingLogPage.tsx untouched
      (found during exploration, noted here as a future cleanup item,
      out of scope for this phase).

   b) New shared utility src/lib/erp/elementCodes.ts: extracted the
      plant element-code generation algorithm (00-<MARK><NN>-<YYMM>M-
      <serial>) out of CastingSchedulePage.tsx as a pure function
      generateElementCodes(drawing, qty, existingCodes, drawings) — the
      ONE piece of real business logic that must not fork between the
      old page and the new one. CastingSchedulePage.tsx was updated to
      import it (the only edit to that file — pure-function swap,
      behavior-preserving, near-zero regression risk).

   c) New page src/pages/CastBedPlanPage.tsx (self-contained, does not
      import from CastingSchedulePage.tsx except the shared util):
        - Grid: rows = production_beds ∪ moulds, columns = a navigable
          7-day rolling window (Prev/Next/Today controls) with Day/Night
          sub-cells per date — genuinely 2D (bed × date) with shift as a
          compact third axis.
        - Draggable cards (@dnd-kit useDraggable) = one per
          casting_schedule row in its (bed, schedule_date, shift) cell.
          Completed/Cancelled rows are locked (not draggable).
        - Droppable cells (@dnd-kit useDroppable) — dropping a card on a
          different cell calls updateAudited('casting_schedule', ...)
          to reschedule bed/date/shift in one action, then reloads.
        - Card click -> detail popover with the same action set as the
          list view (Generate QR / Start Casting / Complete → Stockyard
          / Cancel) — cascade logic on Completion (stockyard_inventory +
          element_traceability inserts) copied verbatim from
          CastingSchedulePage.tsx to keep it identical.
        - Empty-cell click -> Quick Plan modal (project/drawing/qty/
          remarks), pre-filled with that cell's bed/date/shift, using
          the shared generateElementCodes + the same two-insert pattern
          (elements then casting_schedule) as the existing page's form.
        - KPI strip (Scheduled This Week / Beds In Use Today / Completed
          This Week / Utilization %), matching the established
          glass-card-3d tile style.
      Routed at /cast-bed-plan (ProtectedRoute section="planning"),
      sidebar entry added directly after "Casting Schedule" in the
      Planning nav section.

   d) IMPORTANT PRE-EXISTING GAP FOUND (not caused by this phase, but
      discovered while verifying it): production_beds and moulds BOTH
      have 0 rows in the live database, and all 3 existing
      casting_schedule rows have bed="" as a result — meaning even the
      OLD CastingSchedulePage.tsx's bed dropdown has always been empty
      of real options. Separately, the only projects that have drawings
      (P26010, P26011) have NO matching row in the `projects` master
      table at all — so the project->drawing cascade in BOTH the old
      form and the new Quick Plan modal can never resolve for a brand
      new casting with the CURRENT seed data. Neither of these is a bug
      in this phase's code (same cascade logic, deliberately kept
      identical to the proven original) — they're missing/inconsistent
      MASTER DATA that predates this session. The user will need to:
        - populate production_beds / moulds via /m/production-beds and
          /m/moulds (generic CRUD, already works) before the Cast Bed
          Plan grid has any rows to show, and
        - either add projects P26010/P26011 to the projects table, or
          confirm those drawings' project_no values were a data-entry
          mistake and should point at an existing project instead.
      Flagged here rather than silently invented fictional master data.

   e) VERIFICATION (all passed): tsc --noEmit clean, vite build clean
      (CastBedPlanPage chunks separately, 53.57 kB). Playwright E2E
      against the live LAN dev server, 11/11 checks: admin login; nav
      search finds "Cast Bed Plan"; page loads with KPI strip; grid
      renders 54+ empty droppable cells; Quick Plan modal opens/wires
      correctly on an empty-cell click (full insert path not exercised
      live due to the seed-data gap in (d), but shares the exact
      insert shape already proven in CastingSchedulePage.tsx); a
      schedule row temporarily pointed at a test bed (via execute_sql,
      reverted after) rendered as a card in the correct cell; the
      detail popover opened on card click; a REAL mouse-drag gesture
      (pointer down -> move -> up, not just a synthetic event) moved a
      card and was confirmed via direct SQL re-query to have actually
      updated schedule_date in the database — the full drag pipeline
      (drag -> onDragEnd -> updateAudited -> persisted write) is proven
      working end-to-end, not just UI-deep. /casting-schedule confirmed
      still loads correctly (regression check on the one shared-utility
      edit). All test master-data (TEST-BED-1, TEST-MOULD-1) and the one
      temporarily-modified pre-existing row were cleaned up / restored
      to their exact original values afterward — confirmed via SQL
      re-query that the database matches its pre-test state exactly.

   DEFERRED FROM PHASE 2 (explicitly, not forgotten):
     - CastingLogPage.tsx dead-code cleanup (noted, not touched).
     - Month/multi-week calendar views (7-day rolling window only).
     - A true "unscheduled backlog" panel (v1 covers reschedule-via-drag
       + quick-plan-on-empty-cell only).
     - Mobile/touch drag UX tuning (dnd-kit supports touch, but no
       touch-specific polish was done).
     - The production_beds/moulds/projects master-data gaps in (d) —
       these are DATA issues for the user to resolve, not code.

10. V2 ROADMAP — PHASE 3: Element Traceability (real scan + status/defect
    logging). Exploration corrected the premise on two points: (a) QR
    GENERATION already exists and works (CastingSchedulePage.tsx's
    generateQr(), printed via the app's own dependency-free QR encoder,
    lib/qr.ts — nothing to build there); (b) QrScannerPage.tsx
    (/qr-scanner) already existed but its "scanning" was 100% fake — a
    setTimeout animation, no camera, no barcode library anywhere in the
    repo — and it was READ-ONLY, never writing anything. When no
    element_traceability row existed for a scanned code, it silently
    fell back to FABRICATED, hardcoded-looking-real timestamps,
    presenting fake data as if it were a real trace. FULLY COMPLETE AND
    VERIFIED:

    a) Two more load-bearing findings from exploration: element_traceability
       (11 bare text timestamp columns, no audit columns, no per-stage
       user/department/status/remarks — CLAUDE.md's own documented
       per-stage design was never actually implemented) is written by
       THREE different pages (ProductionPage.tsx, CastingSchedulePage.tsx/
       CastBedPlanPage.tsx, DeliveryNotePage.tsx) with NO unique
       constraint on element_code — a latent duplicate-row bug that would
       break QrScannerPage.tsx's .maybeSingle() lookup the moment two
       writers touched the same element. Confirmed via direct query the
       table was empty (0 rows) at the time, so fixable with a zero-risk
       migration before it ever manifested. Also: erection_timestamp/
       completed_timestamp are dead columns, written nowhere in the app.

    b) User decisions (via AskUserQuestion): extend QrScannerPage.tsx IN
       PLACE rather than build a separate page (its scanning was never
       real, so unlike Phase 2's CastingSchedulePage.tsx there was no
       proven behavior to protect). Add html5-qrcode as a new dependency
       for real camera-based decoding (fully client-side, no runtime
       network calls — consistent with the app's LAN-only QR module).
       DEFER photo logging — no Supabase Storage precedent exists
       anywhere in the codebase (confirmed via full-repo search), so
       that's meaningfully separate scope saved for a later pass.

    c) DB migration db/phase3_traceability.sql — applied live:
         - New table traceability_events: the append-only per-stage
           event log that actually fulfills CLAUDE.md's "every stage
           records Timestamp/User/Department/Status/Remarks" design —
           element_code, stage (11-value enum matching the full
           CLAUDE.md lifecycle incl. At Site/Erected/Completed),
           event_type (Scan/Status Update/Defect Reported), status,
           defect_severity (Cosmetic/Minor/Major, reusing repair_works'
           existing vocabulary), defect_description, remarks, logged_by,
           department, full audit columns. RLS reuses the 'stockyard'
           section has_permission() policies (same as element_traceability
           today, since this is co-located functionality on one page).
         - unique constraint on element_traceability.element_code —
           closes the latent duplicate-row bug. Verified via
           get_advisors(security): zero new warnings.

    d) REQUIRED COMPANION FIX, caught during PLANNING (not left to
       verification to discover): the new unique constraint would have
       broken CastingSchedulePage.tsx's and CastBedPlanPage.tsx's
       "Complete -> Stockyard" cascade, which did a BLIND .insert() into
       element_traceability (unlike ProductionPage.tsx/DeliveryNotePage.tsx,
       both of which already look up the existing row before writing —
       verified firsthand by reading their exact call sites, not just
       trusting the exploration summary). Both blind-insert call sites
       switched to .upsert([...], {onConflict:'element_code'}) — a
       one-line, behavior-preserving change per file. VERIFIED via a
       direct SQL simulation of the exact collision scenario (a trace
       row already existing, then the cascade's upsert running against
       it): merged cleanly into one row with the new cascade fields
       applied and the original planning_timestamp preserved — no error,
       no duplicate. This was the precise scenario that would have
       thrown before the fix.

    e) New shared util src/lib/erp/elementTrace.ts: single
       logTraceEvent(params) wrapping insertAudited('traceability_events',
       ...) — mirrors the elementCodes.ts extraction pattern from Phase 2
       (one small shared utility for the one piece of logic that must
       stay consistent).

    f) QrScannerPage.tsx extended in place:
         - Real camera scan via html5-qrcode's Html5Qrcode class
           (facingMode 'environment', started on a "Start Camera" button
           tap rather than proactively grabbing permission on page load).
           On decode, parses the payload exactly as manual entry already
           did (split '|', token[1] = element_code, matching the
           existing SPBM-EL|code|... convention) and reuses handleScan()
           unchanged. Manual USB-gun/type entry stays fully functional
           regardless of camera state — the honest fallback for
           environments with no camera (incl. this sandboxed dev/test
           environment).
         - Fabricated element_traceability fallback REMOVED — a missing
           trace row now shows an explicit "No traceability record found
           for this element yet" banner instead of fake timestamps. (NOTE:
           other fabricated demo data on this same page — QC checklist
           defaults, a hardcoded '2026-06-29' curing-date reference,
           fake driver/trailer names on delivery match — was found during
           exploration but deliberately LEFT UNTOUCHED, out of this
           phase's approved scope. Flagged here, not silently fixed, same
           discipline as leaving CastingLogPage.tsx alone in Phase 2.)
         - New logging actions on the profile card: "Update Status"
           (stage + status, on submit logs a Status Update event AND
           patches both elements.status and stockyard_inventory.status —
           see the bug-fix note below) and "Report Defect" (stage +
           severity + description, logs a Defect Reported event, no
           status change). A "Recent Events" list renders
           traceability_events for the scanned element, reverse-
           chronological.
         - Touch-friendly sizing (min-h-[44px] on interactive controls)
           added for the new elements; the outer grid was already
           grid-cols-1 lg:grid-cols-3 (correctly single-column on mobile
           before this phase too — the real "mobile" gap was the lack of
           real camera input, not the layout).
         - BUG FOUND AND FIXED DURING VERIFICATION (real product bug, not
           a test artifact): first E2E pass showed "Update Status"
           updating elements.status but the profile card's visible status
           chip not changing. Root cause: the chip reads from
           profile.status, which is sourced from stockyard_inventory.status
           — a DIFFERENT table than the one being patched. Fixed by also
           patching stockyard_inventory.status (added profile.stockId,
           captured from the same stockyard_inventory row already being
           fetched). Re-verified after the fix: chip updates correctly.

    g) VERIFICATION (all passed after the fix above): tsc --noEmit clean,
       vite build clean (QrScannerPage chunk grew to 357 kB gzip 105 kB —
       html5-qrcode's bundled decoder — but it's a separate lazy-loaded
       route chunk, not part of the main bundle). Migration applied live,
       traceability_events confirmed with 4 RLS policies, unique
       constraint confirmed live, get_advisors(security) shows zero new
       warnings. Playwright E2E against the live LAN dev server using
       temporary test data (elements + stockyard_inventory rows,
       clearly TEST-prefixed, deleted after): manual-entry scan resolves
       a real profile; honest empty state renders (no fabricated trace);
       Update Status logs the event AND updates the visible status chip;
       Report Defect logs correctly; Recent Events renders both; Start
       Camera fails gracefully in this camera-less sandboxed environment
       without crashing the page (manual entry remains the proven
       fallback path); mobile viewport (390x844) renders a usable
       single-column layout. Separately verified via direct SQL: the
       exact collision scenario the upsert fix was designed to prevent
       (a pre-existing trace row + the cascade's upsert) merges cleanly
       with no error. All test data (elements/stockyard_inventory/
       traceability_events rows, plus a temporary collision-test row in
       element_traceability) deleted afterward — confirmed via re-query.

    DEFERRED FROM PHASE 3 (explicitly, not forgotten):
      - Photo logging + Supabase Storage integration (explicit user
        decision — no precedent exists anywhere in the codebase,
        meaningfully separate scope).
      - erection_timestamp/completed_timestamp on the wide
        element_traceability table remain unpopulated — the new `stage`
        enum on traceability_events covers the full lifecycle including
        At Site/Erected/Completed going forward, so this isn't blocking,
        just noting the old wide table's tail stays historically
        incomplete.
      - Other fabricated demo data on QrScannerPage.tsx (QC checklist
        defaults, hardcoded curing-date reference, fake driver/trailer
        names) — found during exploration, deliberately not touched,
        out of this phase's scope. Worth a dedicated cleanup pass later.
      - Reconciling element_traceability's schema with CLAUDE.md's full
        per-stage spec beyond the new events table (would require
        migrating three existing writers — out of scope here).

11. MASTER-DATA POPULATION (production_beds/moulds/projects), at the
    user's explicit request. Two of the three gaps flagged in items
    9d/10a are now resolved:
      - projects: inserted P26010 and P26011 as minimal, HONEST stub
        records (project_name literally set to the project number
        itself, e.g. project_name='P26010' — not a fabricated real-
        sounding name) since drawings already reference these project
        numbers but no master record existed. User explicitly chose
        this over guessing real project names from the drawing-number
        prefixes (DWG-MGT-.../DWG-AQV-...). Rename via /m/projects once
        real names/clients are known.
      - production_beds: 4 generic entries (Bed 1-4, two Long Line +
        two Battery type, placeholder dimensions) — user explicitly
        chose generic placeholders over providing real plant specs.
      - moulds: 2 generic entries (Mould A/B).
      Also surfaced a pre-existing SCHEMA DRIFT while inserting: the
      live `projects` table has columns id/project_no/project_name/
      location/active(boolean) + audit cols — it does NOT have the
      client/consultant/status columns the registry.ts 'projects'
      ModuleDef implies (those fields would silently no-op or error
      if the Projects CRUD form tries to save them — NOT fixed here,
      flagged as a separate future cleanup item, out of scope for a
      data-population task).
    Verified via Playwright: Cast Bed Plan grid now renders 6 real rows
    (4 beds + 2 moulds) instead of the "No beds or moulds registered"
    empty state — screenshot-confirmed.
    STILL NOT POPULATED (not asked for this round): stockyard_inventory
    is still empty, so QrScannerPage.tsx still can't resolve a real scan
    yet — same category of gap, not yet addressed.

12. STOCKYARD_INVENTORY SEEDING, at the user's explicit request (2026-07-11).
    Found all 3 casting_schedule rows were future-dated (Jul 12-14) and
    unassigned to a bed — zero elements had actually finished casting, so
    stockyard_inventory only had 1 pre-existing real row (00-IW01-2502M,
    project P26002, untouched). Rather than hand-writing fabricated
    stockyard rows, asked the user how to proceed; they chose to backdate
    + complete one real schedule row rather than leave it empty or insert
    disconnected placeholders.
      - casting_schedule row f012c411 (DWG-MGT-WL-101, project P26010,
        6x WL/PC elements 00-IW01-2607M-001..006): bed set to 'Bed 1',
        schedule_date moved from 2026-07-12 to 2026-07-10 (backdated to
        yesterday), status set to 'Completed'.
      - Ran the EXACT SAME cascade CastBedPlanPage.tsx's setRowStatus()
        performs on Completion (mirrored field-for-field from the source,
        not reinvented): elements.status -> 'Cast' + cast_date set;
        stockyard_inventory gets one row per element (status 'Curing',
        bay_location 'Curing Area', remarks 'Cast on Bed 1 (Day shift)');
        element_traceability upserted (onConflict element_code) with
        planning/casting/curing/stockyard timestamps.
      - Verified via Supabase SQL (7 stockyard_inventory rows total: 1
        pre-existing + 6 new) AND via a live Playwright run against the
        LAN dev server: scanned 00-IW01-2607M-001 on /qr-scanner, real
        profile resolved with status chip 'Curing' and no fabricated/
        honest-empty-state fallback shown.
    Remaining 2 casting_schedule rows (DWG-AQV-BW-301, DWG-MGT-HCS-201)
    intentionally left untouched — still future-dated/unbedded, will flow
    into stockyard_inventory naturally as real production proceeds.

13. V2 ROADMAP — PHASE 4: Logistics (2026-07-11). Exploration corrected the
    starting premise substantially: Transport Planning and Crane/Erection
    sequencing were ALREADY BUILT as generic ModuleWorkspace CRUD screens
    (allocations, delivery_schedule, crane_planning, crane_operators,
    erection_planning w/ sequence_no, erection_log) — building new pages
    for these would have duplicated existing functionality. User scoped
    Phase 4 down to the one genuine gap plus a real bug found along the way.
      a) STACK PLANNING — new table `stack_plans` (element_code, project_no,
         planned_bay, planned_position, planned_date, status Planned/
         Executed/Cancelled + audit cols), db/phase4_stack_planning.sql,
         RLS via has_permission() reusing the 'stockyard' section (no
         dedicated 'stack'/'crane' section exists). Registered as module
         `stack-plans` in registry.ts (generic CRUD, same pattern as
         yard-movement/crane-planning), nav entry added under Stockyard.
         Deliberately NOT a custom drag-drop board — matches the roadmap's
         own "list+table UI first" phrasing and the pattern already used
         by every other Logistics module.
      b) STATUS-VOCABULARY RECONCILIATION — found 2 confirmed bugs while
         planning, both fixed:
         - QrScannerPage.tsx's "Update Status" form had `stStatus` as a
           FREE-TEXT input (placeholder "e.g. Erected"), unconstrained,
           even though the adjacent Stage dropdown was already limited to
           the canonical TraceStage list. This was the actual source of
           vocabulary drift — every scan-side status update could write
           an arbitrary new string into both elements.status AND
           stockyard_inventory.status. Fixed: now a <select> using the
           same TraceStage list as Stage.
         - Dashboard.tsx's elementFunnel (donut chart) and projectProgress
           (per-project % bars) were filtering elements.status against
           post-Cast stage names (Curing/Ready/Loaded/Dispatched/
           Delivered/Rejected) that are NEVER written to elements.status
           — only to stockyard_inventory.status. Confirmed via direct SQL
           (00-IW01-2607M-001..006: elements.status='Cast',
           stockyard_inventory.status='Curing' — the funnel was checking
           the wrong column entirely). This silently showed 0% progress
           for every element past "Cast" on every project, always.
           FIXED: both now compute each element's "effective stage" —
           stockyard_inventory.status if a row exists for that
           element_code, else elements.status — via a new
           stockyardStatusByCode lookup map. Verified visually via
           Playwright screenshot: donut now shows a real green "Curing"
           slice, P26010 progress bar now correctly shows 6/18 (33%)
           instead of 0/18 (0%).
         Renamed StockyardPage.tsx's own vocabulary to match TraceStage
         where they represent the same state: 'Ready'->'Stockyard',
         'Loaded'->'Loading' (filter dropdown, edit-modal select, status
         chip colors, AND the Curing Tracker tab filter which would have
         silently dropped curing-complete elements if missed). Also found
         and fixed a THIRD site the original exploration missed:
         registry.ts's 'ready-for-dispatch' module had a hardcoded
         `filter: { field: 'status', value: 'Ready' }` plus its own local
         status options list — both updated to match. Zero data migration
         needed (confirmed via SQL: no existing row held 'Ready'/'Loaded'
         before the rename — all 7 real rows were 'Curing'/'Casting').
         'Rejected' intentionally left as-is — a QC-hold exception state
         outside the forward TraceStage lifecycle, not force-fit into it.
         elements.status's own vocabulary (Planned/QR Generated/Cast) and
         LogisticsPlanningPage.tsx/fleet_status (vehicle location text,
         a different concept) were explicitly left untouched — out of
         scope, no behavioral benefit, would have rippled into
         CastingSchedulePage.tsx/CastBedPlanPage.tsx for nothing.
      Verified: tsc --noEmit + vite build clean; migration applied with
      4 RLS policies confirmed; 8/8 Playwright checks passed against the
      live LAN dev server (module page loads, status form renders as
      select not text input, Stockyard dropdown offers the new option
      and not the old one); Dashboard funnel fix confirmed via screenshot.
      NOTE while verifying: spotted stale TEST-SCAN-001 entries still in
      the Dashboard's Recent Activities feed from Phase 3's earlier
      Playwright run — checked via SQL, the underlying rows (elements/
      stockyard_inventory/traceability_events/element_traceability) were
      already correctly cleaned up; audit_logs is an append-only trail by
      design, so those entries are expected permanent history, not a bug.

14. V2 ROADMAP — PHASE 5: Nine Modules (2026-07-11). Exploration corrected
    the "nine new modules" framing, same pattern as Phase 4: 6 of the 7
    remaining topics (Pre-Stress/PT, Batching, Maintenance, Document
    Control, Handover/DLP, Environmental) already had FULL SCHEMA from
    v2 Phase 1's 22-table data spine + the legacy concrete_batches table
    — registered as generic CRUD with real RLS, but zero custom
    dashboards. Only FORMWORK was a genuine schema gap, same category as
    Phase 4's Stack Planning: `moulds` is a 3-column master list
    (mould_name/type/product_width_m), nothing tracked the actual
    mould-prep workflow. User approved building all 7 in one pass.
      a) FORMWORK — new table `mould_preparation_log` (mould_name, bed,
         activity Setup/Strike/Inspection/Cleaning/Repair, scheduled/
         completed dates, condition, reuse_count, technician, remarks,
         status Scheduled/Completed/On Hold + audit cols),
         db/phase5_formwork.sql, RLS reusing 'production' section.
         Registered as module `mould-preparation` (generic CRUD, same
         treatment as Stack Planning — no custom dashboard). Nav entry
         added under Production, next to Concrete.
      b) SIX NEW KPI DASHBOARDS, all built to the exact
         HSEDashboardPage.tsx/WorkforceDashboardPage.tsx template
         (KPI cards + donut chart + MiniTables + AlertsPanel where a
         genuine due-date field exists in the schema):
         - PrestressingDashboardPage.tsx (/prestressing) — pt_strands/
           pt_tensioning/pt_release/pt_long_line_plans. Alert: pending
           releases (status='Pending', a real wait-state field).
         - BatchingDashboardPage.tsx (/batching) — concrete_batches only.
           NO AlertsPanel — deliberately not fabricating a threshold
           since the table has no due-date concept.
         - MaintenanceDashboardPage.tsx (/maintenance-dashboard) —
           equipment_register/preventive_maintenance/calibration_records
           (the 3 new spine tables the legacy MaintenancePage.tsx never
           touched — that page still owns maintenance_logs only,
           untouched). Alerts: overdue PM, calibrations due ≤30 days.
         - DocumentControlDashboardPage.tsx (/documents) — rfi_register/
           method_statements/submittals. Alert: RFIs open >14 days
           (staleness from raised_date, a genuine signal).
         - HandoverDashboardPage.tsx (/handover) — handover_packages/
           dlp_records/handover_defects/customer_acceptance. Alerts:
           DLPs ending ≤30 days, overdue defects.
         - EnvironmentalDashboardPage.tsx (/environmental) —
           carbon_records/waste_records/water_records/
           environmental_reports. NO AlertsPanel — same reasoning as
           Batching, no due-date field on the tracking tables.
      c) FIXED A NAV COLLISION found while planning: registry.ts already
         labeled the LEGACY MaintenancePage.tsx (a log/calendar form for
         maintenance_logs only) as "Maintenance Dashboard" even though
         it isn't a KPI dashboard and never touched the 3 new spine
         tables. Renamed that entry to "Maintenance Log" (path /maintenance
         unchanged, MaintenancePage.tsx itself untouched) and added the
         real "Maintenance Dashboard" entry pointing at the new
         /maintenance-dashboard page — avoids two identically-labeled
         nav items pointing at different pages.
      All 6 new dashboards' nav entries added as the first item in their
      respective (already-populated) nav groups (prestressing/documents/
      handover/environmental groups; Batching under Production).
      Verified: tsc --noEmit + vite build clean; migration applied with
      4 RLS policies confirmed; 24/24 Playwright checks passed against
      the live LAN dev server (all 6 dashboards load with correct
      titles, zero console/page errors, glass-panel content renders;
      /m/mould-preparation loads; legacy /maintenance still works
      alongside new /maintenance-dashboard; HSE/Workforce/main Dashboard
      regression-checked, unaffected). Screenshot-confirmed the
      Prestressing dashboard's empty state (all 4 spine sections'
      tables are currently empty — no data populated yet, same as when
      Phase 1 first created them) renders as an intentional "No records
      yet" state, not a broken page.
    NOT DONE (explicitly out of scope, no data populated): none of the
    6 new sections' tables have real data yet — dashboards are built and
    verified against empty tables only. Populating Prestressing/
    Batching/Maintenance/Documents/Handover/Environmental with real or
    placeholder records was not requested this phase.

15. V2 ROADMAP — PHASE 6: 3D Visualization Layer (2026-07-11). Exploration
    found the single most important fact for scoping: NO table anywhere
    stores real X/Y/Z coordinates for an element's position — not on
    cast beds, not in the yard, not in erection sequencing. Two
    corrections to that finding, verified via live SQL: production_beds
    DOES have real length_m/width_m (Bed 1/2=30x4m, Bed 3/4=24x3m —
    real seeded data), so bed geometry is real, only element placement
    ORDER within a bed is synthetic; elements.shape_type (added Phase 1)
    is 100% null on all 26 rows (confirmed live), so every element
    renders as a plain box regardless — matches the roadmap's own
    "no detailed shape" non-goal. User approved building all 3 views as
    explicitly ILLUSTRATIVE, schema-honest visualizations (real
    dimensions/weights/status drive appearance; positions are
    procedurally generated where no real spatial data exists — NOT
    presented as a positional record of reality).
      a) New dependency: three@^0.160, @react-three/fiber@^8.18 (NOT
         v9 — v9 requires React 19, this app is React 18, had to pin
         the v8 line after a peer-dependency conflict), @react-three/
         drei@^9.122.
      b) Shared building blocks: src/lib/erp/three/Scene3DShell.tsx
         (Canvas + lighting + OrbitControls + ground grid, frameloop=
         "demand" for efficiency), ElementBox.tsx (box mesh scaled to
         real length_mm/width_mm/thickness_mm, statusColor3D() — a hex
         counterpart to uiHelpers.ts's statusChipClass(), same status
         vocabulary/categorization, Html floating label).
      c) CastBed3DPage.tsx (/visualization/cast-bed) — real bed
         dimensions from production_beds, elements placed along it in
         schedule order (synthetic order, real size), colored by
         casting_schedule status. Bed/date selector.
      d) YardStack3DPage.tsx (/visualization/yard) — bays in an
         invented grid (no bay geometry exists), stacked by
         stack_plans.planned_position where a plan exists else
         insertion order, colored by stockyard_inventory status
         (erection-readiness).
      e) Erection3DPage.tsx (/visualization/erection) — step-through
         UI (Prev/Next, not physics) over erection_planning.sequence_no,
         synthetic pickup/placement points + connecting line, "Crane
         Capacity Check" panel comparing elements.weight_tons against a
         new small CRANE_CAPACITY_TONS constant map (no capacity field
         exists anywhere in the schema — explicitly documented as a
         hardcoded reference, not real engineering data).
      f) Every view carries a consistently-styled amber disclaimer
         banner stating exactly what's real vs. illustrative — same
         "never present synthetic data as real" principle applied
         throughout this project (element_traceability honesty fix,
         master-data placeholder naming, etc.).
      g) Nav: did NOT create a standalone "3D Visualization" nav group
         as first drafted — registry.ts's Sidebar.tsx gates each nav
         GROUP by a single canView(s.key) check against a real RBAC
         section, and 'visualization' isn't one (nor should it be, per
         the plan's own "reuse existing sections" call). Caught via a
         real tsc error (SectionKey type mismatch) before it shipped.
         Fixed by folding each view into its matching existing nav
         group instead: Cast Bed 3D under Production, Yard/Stack 3D
         under Stockyard, Erection Simulation under Site Erection.
      VERIFICATION LIMITATION (documented honestly, not glossed over):
      tsc --noEmit and vite build both clean; 18/18 functional
      Playwright checks passed (canvas element present, zero runtime
      JS/React errors, correct data-driven behavior — e.g. Erection 3D
      correctly shows its empty state since erection_planning has 0
      rows today, real Bed 1 dimensions confirmed in the selector).
      HOWEVER: could not visually confirm final WebGL pixel rendering
      in this sandboxed test environment — the headless Chromium here
      hits repeated "CONTEXT_LOST_WEBGL" driver errors (confirmed via
      console capture, tried both default and --use-gl=swiftshader
      launch flags, tried frameloop="demand" — none fully stabilized
      it). This is a known category of headless/sandboxed-environment
      GPU limitation, NOT a code defect: no JS exceptions were ever
      thrown, and the scene's projection math is demonstrably correct
      (the Html-based floating element-code labels, which are computed
      from the same Three.js camera/scene state, position correctly
      on screen even when the WebGL raster itself fails). Recommend
      the user do a quick visual check in a real browser on real
      hardware before considering this phase fully closed — the code
      is standard, idiomatic react-three-fiber/drei usage with no
      known correctness issues, but this is the one thing sandboxed
      testing genuinely could not confirm.
    NOT DONE (explicitly out of scope): no schema changes this phase
    (by design — every view reads existing tables); erection_planning
    has 0 rows today so the Erection 3D view has never been exercised
    against real multi-step data, only its correct empty state.

16. PHASE 6 EXTENSION: Trailer Loading 3D Simulation (2026-07-11), at the
    user's explicit request — a drag-and-drop check of whether an element
    physically fits on a trailer bed and stays within weight capacity,
    before real dispatch. Exploration confirmed a genuine gap (unlike
    Phases 4/5): trailers had no bed dimensions/capacity anywhere, no
    element-level table ever recorded "loaded on trailer X", and zero
    3D drag-and-drop precedent existed (CastBedPlanPage.tsx's drag-drop
    is @dnd-kit, DOM-based, incompatible with a WebGL canvas).
      a) New table `trailer_types` (type_name, bed_length_mm,
         bed_width_mm, max_weight_tons + audit cols), db/phase6_
         trailer_types.sql, RLS reusing 'master' section. SHIPS EMPTY
         ON PURPOSE — no real trailer specs are known, none invented,
         per this project's standing "never present fabricated data as
         real" principle. Registered as generic CRUD module
         `trailer-types` so real specs can be entered via Master Data.
      b) New src/lib/erp/three/DraggableElementBox.tsx — separate from
         the read-only ElementBox.tsx (kept pure/unmodified for the 3
         existing views). Drag is implemented via R3F's built-in pointer
         events (mesh onPointerDown + a large invisible ground-plane's
         onPointerMove/onPointerUp reading event.point) — NOT manual
         THREE.Raycaster code, which the exploration had flagged as the
         hard part; R3F already raycasts internally per pointer event.
      c) Scene3DShell.tsx gained an `orbitEnabled` prop (default true)
         so OrbitControls can be disabled while dragging — without this,
         the camera would fight every drag gesture. Also added
         frameloop="demand" during this same work for efficiency.
      d) New TrailerLoadSim3DPage.tsx (/visualization/trailer-loading,
         section 'dispatch' — Loading is a Dispatch-lifecycle step per
         CLAUDE.md). Trailer selector -> looks up trailer_types by
         matching type; shows an honest "No capacity data configured...
         add it via Master Data" state if no match (expected today,
         since the table ships empty). Elements sourced from
         stockyard_inventory (Stockyard/Curing status). Live summary:
         weight-vs-capacity badge + out-of-bounds badge. Explicit scope
         limit stated on-screen: checks bed-boundary fit and total
         weight, does NOT detect elements overlapping each other
         (no bin-packing/collision this pass).
      e) REAL BUG FOUND while testing (not caused by this work, but
         blocks this new feature's real-world usability, so worth
         flagging prominently): db/init.sql and db/erp_schema.sql
         define `trailers` DIFFERENTLY (schema drift, pre-existing).
         The LIVE table matches init.sql: plate_no/trailer_type/
         supplier_id(FK)/status. registry.ts's existing `trailers`
         ModuleDef (section 'master', already live before this session)
         has fields plate_no/supplier(ref)/type — matching the OTHER,
         non-live schema file. Confirmed via a direct insert attempt:
         `column "supplier" of relation "trailers" does not exist`.
         This means the Trailers master-data CRUD page has likely been
         broken for saves since before this session — NOT fixed here
         (out of scope, not something this task caused), but it directly
         blocks users from adding real trailers, which is required to
         use the new Trailer Loading Simulation at all. Recommend
         fixing registry.ts's trailers ModuleDef fields (supplier->
         supplier_id, type->trailer_type) as a small, separate, explicitly-
         approved fix. My OWN new page was affected by the same drift
         (originally read trailer.type, silently always undefined) —
         caught and fixed to read trailer.trailer_type before this was
         verified end-to-end.
      Verified: tsc --noEmit + vite build clean both times (before and
      after the trailer_type field fix); Playwright confirmed the honest
      empty state when trailers/trailer_types are empty (5/6 then all
      checks passing after correcting test expectations — trailers WAS
      actually empty too, not just trailer_types); inserted temporary
      trailers + trailer_types test rows (correct real column names:
      plate_no/trailer_type/status) to verify the populated scene —
      bed dimensions (12.0m x 2.5m) and capacity (25T) rendered
      correctly, canvas mounted, no JS errors, 7/7 functional checks
      passed; test rows deleted immediately after, confirmed both
      tables back to 0 rows.
      SAME VERIFICATION LIMITATION AS ITEM 15: a simulated Playwright
      pointer-drag sequence was attempted but was inconclusive — the
      sandboxed environment's WebGL context was lost by that point
      (same CONTEXT_LOST_WEBGL issue as the other 3 views), and the
      synthetic drag likely landed on an HTML label's native text
      selection rather than the 3D mesh underneath. The drag interaction
      itself (not just pixel rendering) has NOT been visually confirmed
      working end-to-end — recommend the user test dragging an element
      onto the bed in a real browser alongside the item-15 spot-check.

17. TRAILERS FIELD-NAME BUG FIX (2026-07-11), at the user's explicit
    request after item 16e flagged it. What started as "fix registry.ts's
    trailers ModuleDef" turned into fixing a live-breaking bug across the
    whole trailers/dispatch surface — 3 separate schema-drift discoveries
    in one pass, all following the same root cause: db/init.sql and
    db/erp_schema.sql define several tables DIFFERENTLY, and the LIVE
    database doesn't consistently match either file. Every fix here was
    verified against the LIVE schema via direct SQL, not the SQL files.
      a) registry.ts's `trailers` ModuleDef: fields renamed supplier(ref)
         -> dropped (see below), type -> trailer_type (now a proper
         select matching the live CHECK constraint: A-Frame/Flatbed/
         Truck Head). Added statusField. Supplier intentionally NOT
         re-added as a ref field: supplier_id is a real UUID FK, but
         ModuleWorkspace.tsx's shared ref-picker (used by every ref-type
         field app-wide) only supports value=label dropdowns — wiring a
         UUID FK through it would show raw UUIDs to users instead of
         supplier names, a regression versus every other ref field in
         the app. Deferred until the picker supports a separate
         labelField; documented inline as a comment, not silently
         dropped without explanation.
      b) Fixed 4 operational pages that all read `t.supplier`/`t.type`
         directly off trailers rows — none of which exist on the live
         table (confirmed: DispatchForm.tsx:163 `t.supplier.toLowerCase()`
         would have THROWN the instant a real trailer row existed, not
         just displayed blank — this was a live crash waiting to happen,
         invisible only because trailers has always had 0 rows):
         DispatchForm.tsx, DeliveryNotePage.tsx, LogisticsPlanningPage.tsx,
         GatePassPage.tsx. Each now fetches `suppliers` (id, name only —
         confirmed live suppliers schema matches this exactly) and joins
         supplier_id -> supplierName client-side, since the shared
         ref-picker limitation in (a) applies here too — no page should
         show raw UUIDs. Also removed a dead `select('id, supplier')` in
         DeliveryNotePage.tsx (line ~258) that would have thrown its own
         query-level error — supplier was fetched but never used.
      c) THIRD discovery, different in kind (not a rename): DispatchForm
         .tsx's "auto-seed dispatch_log from trailers" logic has always
         tried to insert plate_no/supplier_name/trailer_type/driver_name/
         driver_mobile into dispatch_log — none of which existed on the
         live table (only trailer_id/project_no/do_no/shift/status
         flags/remarks/log_date). This silently failed every time
         (network 400, no crash, no seeding) — confirmed via Playwright
         console capture during verification, not assumed. User chose to
         ADD the missing columns (db/phase6_dispatch_log_columns.sql,
         plain `alter table add column if not exists`, pure-additive
         since dispatch_log had 0 rows — no backfill needed) rather than
         strip the fields from the insert, restoring the auto-seed
         feature to work as originally designed.
      Verified end-to-end with real temporary data (not just empty-state
      checks): inserted a real supplier + trailer + triggered the
      auto-seed dispatch_log insert live, confirmed via SQL that
      plate_no/supplier_name/trailer_type all landed correctly
      ("Test Transport Co"/"Flatbed"), confirmed via Playwright that
      DispatchForm now displays the resolved supplier name with zero
      page errors (where before the fix it would have crashed), and that
      all 3 other pages load clean. 7/7 checks passed. All test rows
      (dispatch_log, trailers, suppliers) deleted immediately after —
      confirmed via SQL that dispatch_log/trailers are back to 0 rows
      and suppliers only shows its 1 pre-existing real row ("KANNAN",
      not created by this session).
    NOT DONE (flagged, not fixed): the ModuleWorkspace.tsx ref-picker's
    inability to show a label different from its stored value (item a)
    is a real, small, shared-component limitation surfaced by this work
    but not fixed — it doesn't block anything today (no ref field in the
    app currently needs it), just noting it exists for whenever a UUID-
    FK-backed ref field is next needed.

18. REAL-BROWSER VERIFICATION CLOSED OUT (2026-07-11). User confirmed
    directly ("it's good") that the real-browser spot-check on all 4
    /visualization/* routes passed: the 3D scenes render correctly
    (WebGL, not blank canvas) and the Trailer Loading drag-and-drop
    interaction works. This closes the one verification gap sandboxed
    Playwright testing in this session could not confirm (items 15 and
    16's stated limitation — CONTEXT_LOST_WEBGL in the sandbox prevented
    pixel-level confirmation, code correctness was verified but not
    final visuals/interaction). Phase 6 including the Trailer Loading
    extension is now fully verified end-to-end, not just code-complete.

19. V2 ROADMAP — PHASE 7: Premium UI Pass / Design System Switch
    (2026-07-11). Resolved the design-system conflict flagged since Phase
    1 (CLAUDE.md said "preserve existing red/black glassmorphism branding,
    never redesign" — an earlier pasted plan called for a different
    concrete-grey/blueprint-blue system, never confirmed with the user).
    User chose to adopt the new system as canonical, done in ONE PASS
    (not staged across approval cycles — a deliberate exception to this
    project's usual incremental discipline, made with full scope known
    upfront). This is the largest single undertaking in the project:
    ~50 files touched, 582 automated color replacements, 20 files of
    manual emoji-to-icon conversion, a new dependency, and a CLAUDE.md
    rewrite.
      a) FOUNDATION — tailwind.config.cjs gained real color tokens for
         the first time (surface #F4F4F2, ink #1C1C1E, primary #1B4B8C,
         primary-dark #163C6E, alert #E8622C, success #2C7A6B) and
         fontFamily (heading: Space Grotesk, mono: IBM Plex Mono — the
         mono override cascades free to every existing font-mono
         usage app-wide, zero per-file edits needed for that). index.html
         gained the 2 new Google Fonts links (kept Outfit as body font —
         spec only named headings/mono as deviating). src/styles/
         tailwind.css: rewrote body colors + every red-tinted custom
         class (glass-panel, glass-card-3d, glowing-input, btn-interactive)
         to the new palette IN PLACE — same class names, new colors —
         this was the single highest-leverage move: it re-themed the
         majority of the app's chrome (132+148+54 occurrences across
         35+23+24 files) without touching those files individually.
         Renamed concrete-accent-red/gray -> concrete-accent-primary/
         neutral and glow-text-red/gray -> glow-text-primary/neutral
         (only 1 real usage existed, in ReportsHubPage.tsx — fixed).
         Dead unused CSS classes (status-live, warning-card, safety-
         accent, rev-badge, blueprint-grid, rebar-pattern — confirmed
         zero usages anywhere) left untouched, out of scope.
      b) DARK MODE DROPPED — removed Sidebar.tsx's theme-toggle button
         and its localStorage/useState mechanism entirely. Kept
         `darkMode: 'class'` in tailwind.config.cjs deliberately (not
         removed) so any leftover `dark:` utility classes in the ~35
         files that still have them stay PERMANENTLY INERT (nothing
         ever adds .dark to <html> again) rather than requiring a
         second massive sweep to strip them file-by-file for zero
         functional gain. Verified live: document.documentElement.
         className === '' after login, confirmed via Playwright.
      c) CLAUDE.md rewritten — CORE PRINCIPLES no longer says "preserve
         existing branding / light-dark themes"; UI GUIDELINES gained a
         full "Design System (canonical)" section documenting the exact
         tokens, fonts, icon system and shared chrome classes so future
         sessions don't re-litigate this.
      d) ICON SYSTEM — installed lucide-react (confirmed legitimate
         official package, now versioned 1.x). Built src/lib/erp/
         icons.ts: an EMOJI-KEYED map (not a rewrite of registry.ts's
         127 icon: field values — a deliberate, safer deviation from
         the literal plan text found mid-implementation: keeping the
         emoji as lookup keys into a Lucide map means 0 of those 127
         field values needed touching, only the render call sites).
         Fixed render sites: Sidebar.tsx (both dashboard-link and
         section-button icon spans), ModuleWorkspace.tsx (module header
         icon — cascades to all ~50 generic CRUD pages), PermissionsPage
         .tsx (NAV_SECTIONS-derived table headers).
      e) 20 FILES OF INLINE EMOJI — real per-file work, no shortcut,
         each hand-reviewed to distinguish direct-JSX-literal emoji
         (safe to replace inline) from data-array icon: field values
         (needed a render-site getIcon() lookup instead, to avoid
         corrupting string literals into invalid JSX — a real bug
         caught before it shipped: an early automated regex-based sweep
         script was abandoned mid-design specifically because it would
         have corrupted array literals like Dashboard.tsx's
         `{ icon: '👔' }` TABS array into broken syntax). Files: Sidebar
         .tsx, ImportWizard.tsx, ModuleWorkspace.tsx, AdminPanel.tsx,
         CastBedPlanPage.tsx, CastingSchedulePage.tsx, CsvImport.tsx,
         Dashboard.tsx, DeliveryReportPage.tsx, DispatchForm.tsx,
         GatePassPage.tsx, Login.tsx, LogisticsPlanningPage.tsx,
         MaintenancePage.tsx, PermissionsPage.tsx, ProductionPage.tsx,
         ProjectAutoImportPage.tsx, QrScannerPage.tsx, ReportsHubPage
         .tsx, StockyardPage.tsx, plus the 4 Phase-6 3D pages' "⚠
         Illustrative" disclaimer banners. CastingLogPage.tsx's emoji
         deliberately left untouched — confirmed dead/unrouted code
         (same finding as an earlier session), not worth editing.
         DraggableElementBox.tsx's "✥" drag-cursor hint glyph
         deliberately left as-is — a cursor indicator inside a 3D label
         string, not a branding icon, different in kind from the rest.
         A final broad Unicode-range grep sweep caught 2 more real
         misses outside the original emoji ranges: ⬛/⬜ (QR-generated
         checkbox indicators in CastBedPlanPage.tsx/CastingSchedulePage
         .tsx, fixed to CheckSquare/Square) and ✓/✗ pass-fail badges in
         Erection3DPage.tsx/TrailerLoadSim3DPage.tsx (my own Phase 6
         code, fixed to Check/X for consistency).
      f) COLOR SWEEP — inventoried every distinct red shade actually
         used (300/400/500/600/700/800/950, confirmed via grep, no
         guessing) and ran ONE scripted regex sweep across all of
         src/**/*.tsx (not split into pages/then-components/ batches
         as originally planned — a single controlled pass was simpler
         and equally verifiable): 300/400/500 -> primary, 600/700/800/
         950 -> primary-dark. 582 replacements across 42 files.
      g) BLACK WAS NOT SWEPT — a real scope correction made mid-
         implementation, not a silent plan deviation: investigated
         every plain (non-opacity) bg-black/text-black/border-black
         usage and found NONE of it was actually functioning as a
         "brand color" — it was 100% either (1) printable-document
         templates (Delivery Note/Report labels, correctly black-on-
         white for print fidelity regardless of screen brand) or (2)
         generic neutral chrome (logo-chip backgrounds, unrelated to
         the red/black-vs-blueprint conflict). Left entirely untouched.
         This correction is why the plan's "black -> ink" mapping never
         happened — the premise didn't hold once checked against real
         usage, not because it was skipped.
      h) STRAY HEX COLORS — a targeted (not blanket) pass found and
         fixed genuine brand-identity hex usages the class-name sweep
         couldn't catch: GatePassPage.tsx's printed-pass brand/title
         color, printDoc.ts's shared document-header brand-mark/title/
         section-count colors (used by every printed register/report
         in the app), and main.tsx's config-error fallback screen.
         Explicitly did NOT touch chart data-series fill colors
         (Dashboard.tsx, DeliveryReportPage.tsx Bar/Pie/Line charts) or
         semantic status-color maps (KANBAN_COLUMNS, SEVERITY_COLORS,
         PIE_COLORS, GRADE_COLORS, WASTE_COLORS, Erection3D/
         TrailerLoadSim3D's pass-fail red) — same "leave semantic pass/
         fail colors alone" principle applied throughout, confirmed via
         reading each site's actual context rather than assuming.
      Verified: tsc --noEmit + vite build clean after EVERY stage
      (foundation, icons, color sweep, residual fixes — not just once
      at the end, given the size of this change). Final residual grep
      confirmed ZERO remaining red-[0-9] Tailwind classes anywhere in
      src/. 22/22 Playwright checks passed against the live LAN dev
      server across a representative cross-section of pages from every
      phase built this session (Dashboard, a generic /m/ module,
      CastBedPlanPage, QrScannerPage, a Phase-5 KPI dashboard, a Phase-6
      3D view, HSE, Stockyard, Reports Hub) — zero page errors anywhere.
      Confirmed live via computed style: nav elements render at
      rgb(27, 75, 140) = exactly #1B4B8C, the new primary token, not a
      leftover red. Confirmed via document.documentElement.className
      that dark mode is never applied. Screenshot-confirmed the overall
      visual result: consistent blueprint-blue accent app-wide, clean
      Lucide line icons throughout the sidebar and every module page
      (confirmed the ModuleWorkspace.tsx fix cascades correctly to all
      ~50 generic CRUD pages via one file), concrete-grey background.
    NOT DONE (explicitly out of scope, not silently dropped): the
    "compact by default, comfortable toggle in settings" density
    feature named in the original design spec was NOT built this pass
    — it's a genuinely new settings feature (new state, new toggle UI),
    not a re-theme of existing pages, and was never explicitly
    confirmed as in-scope for this pass. Flagged for a future request
    if still wanted.

20. V2 ROADMAP — PHASE 8: Reporting/Analytics Gap-Check (2026-07-11) —
    THE FINAL PHASE of the 8-phase v2 roadmap. Bundled 4 sub-features in
    the original roadmap text; each got real scoping against actual code
    before building, same discipline as every phase this session:
      a) COSTING/INVENTORY VALUATION — SKIPPED ENTIRELY, on purpose. The
         roadmap literally said "cost estimation tied to Batching/Raw
         Material," but CLAUDE.md's OUT OF SCOPE section explicitly
         names "Costing" and "Inventory valuation" as PERMANENTLY
         EXCLUDED modules. Confirmed via schema grep: zero price/rate/
         cost columns exist anywhere (bom_items/mix_designs/
         concrete_batches are quantity-only). Unlike Phase 7 (which
         deliberately overrode a stale CLAUDE.md rule with explicit user
         sign-off), this rule was left standing exactly as written —
         no schema, no UI, no CLAUDE.md change. This is the correct
         precedent going forward: not every roadmap-vs-CLAUDE.md
         conflict resolves the same way; each needs its own explicit
         decision.
      b) MOBILE COMPANION (Element Control) — CONFIRMED ALREADY
         SATISFIED, not built. The app is already a fully responsive
         web app (Sidebar.tsx's mobile drawer, QrScannerPage.tsx's
         touch-target-sized responsive grid from Phase 3). No PWA/
         installability scaffolding exists (no manifest, no
         vite-plugin-pwa) and none was added — user explicitly decided
         the existing responsive experience already counts. Verified
         via REAL Playwright testing at a 390×844 viewport (not
         assumed): Dashboard/QR Scanner/a generic module page all show
         zero horizontal overflow, the mobile hamburger menu and
         sidebar drawer both work correctly.
      c) CUSTOM REPORT BUILDER — built. ReportsHubPage.tsx had 15
         hardcoded reports via one buildReport() switch, zero
         user-driven query composition. New: db table `custom_reports`
         (name/source_table/columns jsonb/filters jsonb + audit cols,
         RLS reusing the existing 'reports' section — no new section
         needed). New src/lib/erp/reportTables.ts extracts
         ReportsHubPage.tsx's curated 26-table TABLES array into a
         SHARED module both pages import — one list, not two to drift
         out of sync (ReportsHubPage.tsx edited to import it instead of
         defining its own copy). New ReportBuilderPage.tsx
         (/reports-builder): source-table dropdown, column picker
         (real labels from the matching registry.ts ModuleDef when one
         exists, else sample-row keys), a repeatable filter builder
         (equals/contains/gte/lte, AND-combined), and a preview that
         renders through the EXACT SAME ReportSection/exportCsv/
         exportExcel/printSectionsDoc pipeline ReportsHubPage.tsx
         already uses — zero new export code written. Save/reload
         re-queries LIVE data on rerun, never shows stale cached rows.
      d) SHOP DRAWING VIEWER — built, with a real security fix along
         the way. drawings table was metadata-only (confirmed live via
         direct schema query, matches db/erp_schema.sql exactly this
         time — no drift found). Added drawings.file_url (additive).
         FIRST REAL USE of Supabase Storage in this app (confirmed
         zero prior usage anywhere, despite being named in CLAUDE.md's
         intended tech stack) — a new 'drawings' bucket + RLS on
         storage.objects gated by the existing 'design' section.
         SECURITY CATCH: my first draft set the bucket public:true for
         simplicity — the environment's own safety classifier correctly
         blocked this, flagging that a public bucket lets anyone with a
         file URL view confidential project drawings with NO login,
         completely bypassing the app's own RLS/permission system. User
         confirmed: private bucket + signed URLs instead. file_url now
         stores the STORAGE OBJECT PATH, not a public link;
         DrawingViewerPage.tsx (/drawings/viewer) requests a fresh
         300-second signed URL client-side every time a drawing is
         viewed, so access always goes through Supabase auth + RLS,
         never a bare unauthenticated URL. Renders images via <img>,
         PDFs via <iframe> (browser-native, no new PDF library needed).
         Deliberately built as its own page, NOT a rewrite of the
         generic ModuleWorkspace CRUD component — file upload isn't a
         supported FieldDef type there and adding one for this single
         need was out of scope.
      MID-SESSION SUPABASE MCP RE-AUTH INCIDENT: the connection expired
      again mid-planning (same "Unrecognized client_id" OAuth issue as
      earlier this session). This time my own attempt to run
      `claude mcp remove supabase -s project` was auto-blocked by the
      environment's safety classifier as an "unrequested self-
      modification" — a correct call, since I hadn't yet confirmed with
      the user that THIS was the right fix for THIS specific error. Only
      proceeded after the user explicitly confirmed the exact same
      "Unrecognized client_id" text and explicitly approved the remove/
      re-add fix. Worth remembering: the classifier will block
      previously-approved actions again if context doesn't clearly show
      the user re-requested it in the current situation — that's
      correct behavior, not a bug to work around.
      Verified: tsc --noEmit + vite build clean. 16/16 Playwright checks
      (report builder full round-trip: compose → preview → save →
      appears in My Reports, zero page errors; drawing viewer loads
      clean; 3 mobile-viewport pages all overflow-free; hamburger +
      drawer work) PLUS a separate 4/4 real-file-upload test (uploaded
      an actual test PDF through the live UI, confirmed it round-tripped
      through Storage and rendered inline via signed URL in an iframe —
      not just checked that the button exists). ALL TEST DATA CLEANED
      UP: confirmed via SQL that custom_reports is back to 0 rows,
      drawings.file_url is back to all-null (the one pre-existing real
      drawing row touched — DWG-AQV-BW-301 — was explicitly reverted to
      its original null value, isolated from the bulk test-data
      cleanup, same discipline as earlier sessions' pre-existing-row
      handling), and the Storage bucket is empty (removed via the
      Storage API through the app's own authenticated client, since
      direct SQL deletion from storage.objects is blocked by Supabase's
      own protect_delete() trigger — a real safety feature, not a bug).
    THIS CLOSES THE FULL 8-PHASE V2 ROADMAP. Phases 1-8 are all done.

=====================================================================
V2 ROADMAP — REMAINING PHASES (context for continuation)
=====================================================================

NEW ROADMAP — Phase structure (8 phases total):
  Phase 1: Core data spine — elements table gaps, "18 missing tables"
           (list NOT actually provided — see reconciliation note below),
           RLS/permissions fix on existing tables. Depends on: nothing.
  Phase 2: Production planning — production_casting, Cast Bed Plan
           (2D grid, drag-drop, calendar-linked). Depends on: Phase 1.
  Phase 3: Element traceability — QR generation, mobile scan flow,
           status/defect/photo logging. Depends on: Phase 1, 2.
  Phase 4: Logistics — Stack Planning, Transport Planning,
           Crane/Erection sequencing (list+table UI first).
           Depends on: Phase 2, 3.
  Phase 5: Nine new modules — Pre-Stress/PT, HSE(DONE), Formwork,
           Batching, Maintenance, Document Control, Workforce
           Scheduling(DONE), Handover/DLP, Environmental.
           Depends on: Phase 1.
  Phase 6: 3D Visualization layer (NEW addition vs. the user's original
           plan) — Three.js/react-three-fiber element renderer:
             - Cast Bed 3D view (elements on bed/mould, colored by status)
             - Yard/Stack 3D view (colored by erection-readiness)
             - Erection sequence simulation (step through erection IDs,
               simplified crane capacity box, no real physics)
           Explicit non-goals: no rebar/reinforcement 3D detail, no
           Revit/Tekla/IFC import, no physics-based lift simulation.
           Depends on: Phase 2, 3, 4.
  Phase 7: Premium UI pass — design system enforcement, empty/loading
           states, dashboard, mobile companion app shell.
           Depends on: all above.
  Phase 8: Reporting/analytics + final "no other software needed" gap
           check (shop drawing viewer, cost estimation tied to
           Batching/Raw Material, custom report builder, mobile
           companion covering at least Element Control).
           Depends on: all above.

NEW DESIGN SYSTEM SPEC (locked in, restated in the pasted plan — NOTE:
this is a DIFFERENT visual language than the red/black glassmorphism
CLAUDE.md currently specifies and the app currently uses — this
conflict has NOT been resolved/discussed with the user yet):
  Base: concrete-grey #F4F4F2 bg / #1C1C1E text
  Primary accent: blueprint-blue #1B4B8C
  Alert accent: safety-orange #E8622C
  Success (new): muted teal #2C7A6B
  Headings: Space Grotesk. Data/codes/tables: IBM Plex Mono.
  Grid metaphor: "Cast Bed Plan" grid used as the general layout language
  Density: compact by default, "comfortable" toggle in settings
  Icons: Lucide, one consistent set

  *** OPEN CONFLICT NOT YET FLAGGED FOR RESOLUTION: CLAUDE.md says
  "Always preserve existing UI/UX / branding ... Never redesign completed
  pages unless absolutely necessary." The new plan's Phase 7 explicitly
  calls for a different color system/typography than what's live today.
  This needs an explicit decision from the user before Phase 7 starts:
  is the new visual system replacing CLAUDE.md's red/black glass system,
  or is it aspirational language from the other planning session that
  should be reconciled back to the existing design system? ***

SCHEMA STATUS as of v2 Phase 1 completion: `elements` now has shape_type
+ full audit columns + real RLS (all confirmed live). All 22 new tables
across the 6 previously-empty categories exist with real RLS + RBAC.
The "18 missing tables" figure from the original pasted plan turned out
to be inaccurate once derived against the live schema (22 tables were
actually needed; 3 tables the plan assumed missing already existed) —
see item 8 above for the full derivation record.

=====================================================================
NEXT STEPS (in order)
=====================================================================
1. ALL 8 PHASES OF THE V2 ROADMAP ARE DONE AND COMMITTED (677d011 =
   Phase 8, the final one: custom report builder, shop drawing viewer
   with private Storage bucket + signed URLs, costing explicitly
   skipped per CLAUDE.md, mobile companion confirmed already-satisfied).
2. DONE (2026-07-11): fixed a real data-consistency issue in the one
   piece of trailer data that existed — trailers.trailer_type said
   "A-Frame" but trailer_types.type_name said "A-FRAME" (mismatched
   casing from independent data entry). Updated trailer_types to match
   trailers' casing. Note: TrailerLoadSim3DPage.tsx's lookup was
   already case-insensitive (.toLowerCase() both sides) so this wasn't
   actually blocking the simulation — but casing consistency matters
   for any other place that reads trailer_type as a display label or
   does an exact match later, so the fix was still worth making.
3. USER EXPLICITLY DECLINED (2026-07-11) to populate the other 20 empty
   tables (Prestressing: pt_strands/pt_tensioning/pt_release/
   pt_long_line_plans; Maintenance: equipment_register/
   preventive_maintenance/calibration_records; Document Control:
   rfi_register/method_statements/submittals; Handover:
   handover_packages/dlp_records/handover_defects/customer_acceptance;
   Environmental: carbon_records/waste_records/water_records/
   environmental_reports; erection_planning; mould_preparation_log) —
   said "skip the rest for now." NO PLACEHOLDER DATA WAS INVENTED for
   any of them, per this project's standing "never fabricate realistic
   business data" rule. All 6 Phase-5 KPI dashboards and the Erection
   3D view remain honestly showing their empty states until the user
   provides real specs. Do not revisit this unprompted — wait for the
   user to bring real data for a specific area.
4. DONE (2026-07-11): imported real project data from 3 Excel files the
   user placed in the repo folder (25045 - Acres Boundary Wall Progress
   Report.xlsx, P25034-Ammara BW Progress.xlsx, P25020 -Progress report
   (based on physical inventory).xlsx). Every row traces to a real
   source cell — no fabricated/placeholder data.
   - Created 3 `projects` rows: P25045 (Acres Boundary Wall), P25034
     (Ammara BW), P25020 (SAN Campus, later renamed — see below) — all
     new, per explicit user decision to keep them separate from the
     existing similarly-named P25044 (Acres Phase 1).
   - P25034: 1111 elements, 248 delivery-note groups, 604 erection
     events, all with real dates/volumes from the Cast/Del/Ere sheets.
   - P25045: 2575 elements (2291 with real dimensions/weight from the
     file's `Data` sheet — WIDTH/HEIGHT mapped to length_mm/width_mm,
     THICKNESS to thickness_mm; this mapping is a disclosed assumption,
     not verified against the original drawings), 58 delivery groups,
     188 erection events (some undated — the source file's `Ere` sheet
     itself lacks a date for ~1/3 of rows; left null rather than
     invented). Literal '#ERROR!' cells in the source Cast sheet's
     VOL/BAY columns were treated as null.
   - P25020: fundamentally different file shape — a physical-inventory
     snapshot, not a dated event log (confirmed via read_only=False,
     zero merged cells: the Cast/Del/Ere. sheets genuinely have no
     dates in any row). Its "Element ID" is a mark/type code with a
     quantity (up to 16), not a per-piece code like the other two
     files. Per explicit user decision, each mark+qty row was EXPANDED
     into N serialized `elements` rows (e.g. ...SW-7M-08-01 through
     -16) — these serial suffixes are synthesized, the one place this
     import invents identifiers rather than copying them. Status per
     expanded piece was assigned via an explicit per-mark distribution
     rule (erected pieces first, paired with real `Ere.(Site)` dates;
     then delivered; remainder cast-only) so piece-level dashboard
     counts stay correct rather than just duplicating one status across
     all N pieces. Result: 4274 elements, 2917 real dated erection
     events (from `Ere.(Site)`, the only dated sheet in this file), 0
     deliveries rows (the `Del` sheet has no dates to log — folds into
     element status only).
   - Load mechanism: NOT the in-app ImportWizard (single-table CSV
     tool, no cross-table fan-out) and NOT raw service-role SQL text
     round-tripped through the assistant's own context (hit an ~30KB
     per-call display ceiling well before the row counts needed).
     Instead, a Playwright script drove the ALREADY-AUTHENTICATED
     admin browser session's real `supabase` client in-page (same
     pattern as the Phase 8 Storage cleanup script) to insert in
     chunks of 500 — goes through real RLS as a real logged-in user,
     not a bypass. Every row stamped `created_by/updated_by =
     'xlsx-import-P25045' / '-P25034' / '-P25020'`. Fully reversible
     per project: `delete from <table> where created_by =
     'xlsx-import-P25034'` (etc.) across elements/deliveries/
     erection_log/element_traceability, tested for real on disposable
     rows before the full load (insert 1 row per table, delete by tag,
     confirm zero remain) — not just asserted safe.
   - IMPORTANT FINDING, resolved by explicit user decision: P25020 and
     P25034/P25045 all already had PRE-EXISTING `deliveries` rows
     (created_by=null) before this import — the `projects` master
     table had no row for them, but child tables did (a real, pre-
     existing data-integrity gap in this ERP, not something this
     import caused). For P25034/P25045 those pre-existing rows are the
     SAME real projects (project_name "AMARA BW" / "ACERS BW") — no
     conflict, this import's rows are simply additional real history.
     For P25020 the pre-existing 78 rows were named "KU CLADING" with
     element_type='CLADDING' — a different-looking scope of work. User
     was asked directly and confirmed: KU Cladding and SAN Campus ARE
     the same real project — left as one project_no. User then chose
     "KU CLADING" as the canonical project_name (2026-07-11); the
     `projects` row was updated from the initial "SAN CAMPUS" to match
     the pre-existing delivery rows' naming.
   - `element_traceability` (casting/delivery/erection_timestamp) was
     stamped per element from the same dates already computed above —
     one row per element, matching that table's one-row-per-element
     schema (it is not a repeatable event log; erection_log and
     deliveries are the real repeatable tables in this schema).
   - Verified: `get_advisors` clean (no new findings — all pre-existing
     architecture items unrelated to the tables touched); live-app
     spot check confirmed a real P25034 element (AMR-V17-T1C10-001) is
     searchable in Element Register with the correct "Erected" status.
     Separately verified P25020's per-mark expansion directly against
     one real qty=11 mark (25020-1F-B7-SW-7M-08): 10 pieces Erected
     with real distinct Ere.(Site) dates, 1 Delivered with no invented
     date, correct Erected-then-Delivered ordering — confirms the
     distribution rule executed correctly, not just that the aggregate
     counts happened to add up. Also verified P25045 elements carry
     real per-piece dimensions/weight/cast dates from the file's `Data`
     sheet (e.g. 5315x1750x120mm, 2.9925T).
5. Other explicitly-deferred items, none requested yet: the
   photo-logging follow-up from Phase 3, the density-toggle ("compact
   by default, comfortable toggle") feature deferred from Phase 7.
6. Worth a dedicated pass sometime: db/init.sql and db/erp_schema.sql
   have diverged on at least 4 tables (trailers, suppliers,
   dispatch_log, and earlier-session findings on others) — the live DB
   doesn't consistently match either file. Each phase that's touched a
   drifted table has fixed the specific symptom it hit (item 17's
   trailers/dispatch_log fix, Phase 8's confirmed-clean drawings check),
   but the underlying two-source-of-truth problem in db/*.sql itself is
   still there for whatever table gets touched next.
7. With the roadmap complete and 3 real projects now populated (item
   4), worth asking the user directly: is there a Phase 9 / new
   initiative, or is the ERP considered feature-complete for now
   pending further real-world data entry (the 20 tables from item 3,
   or more Excel files for other projects)?

=====================================================================
KNOWN TROUBLESHOOTING NOTES (carry forward)
=====================================================================
- Vite dev server can silently fail to hot-reload changes to widely-
  imported files (registry.ts is the confirmed case). Symptom: code is
  correct on disk but the running app doesn't reflect it, no HMR log
  line for that file. Fix: pkill -f "vite --host" then restart
  scripts/dev-lan.sh.
- Always sync BOTH /mnt/Gemini Cli/build and /root/build copies for any
  edit — use tar with -C flags, never cd first (caused several failed/
  reversed syncs earlier in the project).
- Playwright is the only available UI verification method this session
  (Chrome extension not connected). Installed at
  /root/build/.ds-sync/node_modules/playwright/index.mjs from earlier
  /design-sync work.
